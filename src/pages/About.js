import React from 'react';

import exampleGraphImage from '../images/example.png';

const About = ({ action }) => {
    return (
        <div className='About'>
            <h2>Create stunning visualizations of your Spotify listening history.</h2>
            <p>Ripplify is a tool that helps you examine your music taste as a proxy for self-discovery.</p>
        </div>
    );
};

export default About;
